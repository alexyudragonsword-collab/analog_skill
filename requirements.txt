# Runtime dependencies.  Upper bounds are deliberate: every one of these
# has shipped a breaking major release before, and a frozen build that
# silently picks one up fails at the user's desk, not in CI.  Raise a bound
# only together with a green run of the test matrix.
numpy>=1.24,<3
matplotlib>=3.7,<4
scipy>=1.10,<2
PySide6>=6.6,<7
