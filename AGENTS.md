# Analog Studio Collaboration Rules

> This project uses Project Cairn to organize project knowledge: `AGENTS.md` is the rules-and-navigation entry point, `cairn/` is the project knowledge/state layer.
> The `CLAUDE.md` in the same directory should contain only one line, `@AGENTS.md`, so Claude Code reads the same rules; Codex reads this file directly.

## Project one-liner

A PySide6 desktop workbench for analog IC design, driving ngspice locally — gm/ID sizing, device characterization across 40 PTM models, block-level circuits, and automatic device sizing over 27 SKY130 / PTM topologies.

> This file was generated by `cairn init`, already populated with this project's own positioning and provider config; other projects should run their own init before reusing it.

## Init configuration

- Graduation provider(s): none yet (deferred — connect a knowledge base at first graduation)
- Knowledge base index: not configured yet
- Graduation target: not configured yet

## Reading order after entering the project

1. Read this file (AGENTS.md) first.
2. Read `ROADMAP.md` **in the repository root** for open work, what is blocked on whom, and what was deliberately decided against. This project keeps its roadmap at the root rather than in `cairn/` — it predates Cairn and is referenced by `README.md` and `CONTRIBUTING.md`.
3. Read the most recent entries in `cairn/LOG.md` (newest entries are at the top) for recent progress and key decisions.
4. Read the relevant `cairn/` topic notes as needed for the task at hand — `cairn/pitfalls.md` is worth reading before touching the sizing package, the workspace paths, or the schematics.

## Document responsibilities

| File | Role | Maintenance |
|---|---|---|
| `AGENTS.md` (root) | Rules and navigation | Rarely changes |
| `CLAUDE.md` (root) | One-line `@AGENTS.md` stub | Written once, never touched again |
| `ROADMAP.md` (root) | Roadmap and open work | Updated in place, kept concise |
| `cairn/LOG.md` | Chronological log | New entry added at the top (newest first), each entry ≤ 20 lines, summary + pointer only |
| `cairn/<topic>.md` | Knowledge topic note (current truth) | Updated in place; pitfalls go in a body section, tagged via `contains`; revisions get a LOG pointer |
| `cairn/Reference/` | External raw input | Created as needed; append-only |
| `cairn/Cited.md` | Knowledge base citation list | Pointers only, never copies of the source |
| `CHANGELOG.md` (root) | Released/shipped changes | One entry per change, in the same commit |
| `CONTRIBUTING.md` (root) | Developer workflow | Tests, lint, schematics, CI, release |

> Everything else is created only when a concrete signal calls for it (a decision needs to be recorded, a pitfall gets solved, a goal outlasts one session) — not pre-built as empty shells. Engineering assets (contracts/config/specs consumed by code or process) are not managed by this system; they stay in the code tree, not in `cairn/`.

## Hard rules (this project)

- **Never modify the vendored trees**: `analoggym/`, `circuit-skills/`, `gmoverid/`, `ngspice/`, `transistor-models/`. They are upstream snapshots the app reads from disk as shipped, and dated analyses cite them. Adapt around them in `app/`.
- **Never run two test suites concurrently.** They share the ngspice scratch directory and the user-data store; a second run produces failures that look real but are not. Re-run a surprising failure alone before diagnosing it.
- **Do not weaken the source-leak gate** in the `linux-nuitka` CI job — no `app/` `.py`/`.pyc` in the distribution is the entire point of the Nuitka route.
- **Both checks must pass before pushing**: `python -m pytest app/tests/ -v` and `python -m ruff check .` — CI runs exactly these, plus the same tests on Windows and macOS.
- Commit and push only to the branch you were told to use. Do not push tags or open pull requests unless asked.

## Commands

```bash
python -m app.main                      # run (QT_QPA_PLATFORM=offscreen if headless)
python -m app.main --smoke              # build every tab, exit 0
python -m pytest app/tests/ -v          # ~86 tests, ~2.5 min, real ngspice
python -m ruff check .                  # must be clean; config in ruff.toml
python tools/gen_sizing_schematics.py   # redraw the 20 Sizing schematics
```

## Layout

```
app/core/sizing/   the Sizing engine, split by responsibility; import graph is
                   strictly one-directional and verified acyclic:
                   spec -> registry -> assets -> scoring -> evaluation ->
                   user_circuits -> report -> optimizer -> runs -> plots
app/core/          worker (single background thread), circuits, gm/ID services,
                   LLM client, ngspice locator, model registry
app/ui/            main_window + six tabs; job_mixin.py is the shared
                   submit/cancel/failure protocol every tab uses
app/paths.py       frozen-vs-source path resolution, workspace sync, user data
tools/             schematic generators (drawings are rendered from netlists)
```

## Conflict arbitration rules

- Priority: **topic notes > LOG history**; rule-level conflicts are resolved by this file.
- Business/design conclusions follow the latest record in the `cairn/` topic notes, not older LOG entries.

## Knowledge base consumption reflex

- Before work whose reusable kernel — any conclusion it produces or depends on — would be graduation-worthy, consult this project's own `cairn/` topic notes; no external knowledge base is connected yet (provider deferred — see Init configuration above), so the external index check and `cairn/Cited.md` citations activate once one is connected.

## Document collaboration rules

- Before making a change, judge whether the user wants "discuss/suggest" or "just edit the doc directly"; when they say "take a look first / evaluate first," give analysis first — don't rewrite a formal doc outright.
- When correcting a past judgment, append a correction note; don't silently overwrite it.
- Don't write an unconfirmed judgment as a settled fact.

## Knowledge distillation rules

- After every substantive step forward, add one entry to the top of `cairn/LOG.md` (summary + pointer); let conclusions settle into the `cairn/` topic notes.
- **Completion reply gate:** before any completion claim—including but not limited to work being complete or implemented, finalized, updated, synchronized, verified or tests passing; a problem being fixed or resolved; a deliverable being ready to use; a statement that work has ended; and semantically equivalent wording—run the Cairn checkpoint in `references/maintenance.md`; update only the records its trigger matrix requires, verify them, then reply. An explicit read-only / no-edit request forbids Cairn writes.
- Cross-project reusable experience gets distilled via the graduation mechanism once a knowledge base is connected (provider deferred — see Init configuration above).

## Code conventions

- Comments say **why**, not what; match the density of the file you are in.
- ~79 columns. `l` is channel length and `I` is current because the netlists say so; the drawing modules use `d += a; d += b`. `ruff.toml` documents why those rules are off — follow the local style, do not import your own.
- Update `CHANGELOG.md` in the same commit as the change it describes.
- No `TODO` comments: open work goes in `ROADMAP.md`.
