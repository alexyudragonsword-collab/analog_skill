* StrongArm Comparator — Statistics Testbench
* VDD={VDD}V, CLK=1GHz, Noise BW=10GHz
* Vin_diff = {vin_label} mV, na={noise_na_uv:.0f}uV per side, nt={noise_nt_ps:.0f}ps

* ── Supply and clock ──────────────────────────────────────────────────────────
VVDD VDD 0 DC {VDD}
VCLK CLK 0 PULSE(0 {VDD} 0 10p 10p 500p 1n)

* ── Input stimulus ────────────────────────────────────────────────────────────
{inp_src}
{inn_src}

* Input-referred noise (BW = 10 GHz, nt = 50 ps)
VN_P INP INP_DC trnoise({noise_na} {noise_nt} 0 0)
VN_N INN INN_DC trnoise({noise_na} {noise_nt} 0 0)

* ── DUT ───────────────────────────────────────────────────────────────────────
{dut_include}

X1 INP INN CLK VDD OUTP OUTN comparator_strongarm

* ── Initial conditions (reset state) ─────────────────────────────────────────
.ic V(x1.vxp)={VDD} V(x1.vxn)={VDD} V(x1.vlp)={VDD} V(x1.vln)={VDD} V(x1.vs)=0

.include {model_path}

.control
tran {tstep} {tstop} uic
wrdata {out_outp} v(outp)
{ivdd_cmd}
.endc

.end
