# development / packaging only — end users do not need these
# (same upper-bound policy as requirements.txt)
pytest>=7,<10
# lint gate (.github/workflows/test.yml); pinned to one minor line so a
# new ruff release cannot turn CI red without a deliberate bump
ruff>=0.16,<0.17
pyinstaller>=6,<7
# Nuitka 4.x's pyside6 plugin ships dists without any PySide6/shiboken6
# extension modules (verified 4.1.3: loader then aborts/segfaults at
# startup); the 2.x series packages PySide6 correctly — keep it pinned.
nuitka>=2.8,<3
# only for regenerating the pre-rendered circuit schematics
# (tools/gen_schematics.py); not a runtime dependency.  Bounded to the 0.x
# line: schemdraw renders are version-sensitive (0.23 reproduces the
# committed PNGs pixel-for-pixel except for a 1-px canvas width on
# ldo_folded_cascode), so a major bump would rewrite the committed images.
schemdraw>=0.19,<1
# optional TPE algorithm for the Sizing tab (source runs only; the
# frozen builds ship the built-in Sobol+Powell optimizer)
optuna>=3.0,<5
